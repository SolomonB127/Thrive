name: thrive
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider:
  isar:
  isar_flutter_libs:
  path_provider:
  intl:
  flutter_slidable:
  flutter_heatmap_calendar: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  isar_generator:
  build_runner:

flutter:
  uses-material-design: true
  assets:
    - assets/
